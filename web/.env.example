# Copy to .env.local (never commit .env.local).
# cp .env.example .env.local

# Gateway the Next.js server calls. The browser never sees these.
GATEWAY_URL=http://localhost:8080
BIFROST_API_KEY=dev-local-key

# 1 = skip the gateway (fake chat + fake /preview). Demo slider still works.
# 0 = call GATEWAY_URL /v1/chat/completions and /v1/route/preview
BIFROST_MOCK=1

# Dashboard sample-data fallback. Real rows always win when the DB has them;
# this only decides what shows when /api/* is empty or unreachable.
# 1 = fall back to generated sample traffic (default)
# 0 = show empty/error states instead
NEXT_PUBLIC_BIFROST_DEMO=1
